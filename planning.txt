models?
routes?

voting app functionality

1.user sign in/sign up(with the help of authentication)
2.see the list of candidate
3.vote one of the candidate, after voting, user can't vote
4.there is the route which shows the list of the candidates and there live votes count sorted by their vote count
5.user data must contain their one unique id proof named: aadharCardNumber
6.there should be only one admin who can maintain the table of candidates and he can not vote at all
7.user can change their password
8.user can only login with their addhar card number and password

---------------------------------------------------------------------------------

routes?

user authentication:
/signup: POST - create a new user account
/login: POST - log into existing account [addharNumber + password]

voting:
/candidates: GET - get the list of candidates
/vote/:candidateId: POST - vote for specific candidate

vote counts:
/vote/counts: GET - get the list of candidates sorted by their vote counts

user profile:
/profile: GET - get the user's profile information
/profile/password: PUT - change the user's password

admin candidate management
/candidates: POST - create a new candidate
/candidates/:candidateId - PUT - update an existing candidate
/candidates/:candidateID - DELETE - Delete the candidate from the list